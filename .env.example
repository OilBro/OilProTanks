# Environment variables example for OilProTanks
# Copy to .env (root) and adjust values as needed.

# === SERVER CONFIGURATION ===
NODE_ENV=development
PORT=5000

# === DATABASE ===
# PostgreSQL connection string
# For local development: postgresql://username:password@localhost:5432/database_name
# For Render deployment: Automatically set via render.yaml
DATABASE_URL=postgresql://username:password@localhost:5432/oilprotanks

# === AUTHENTICATION ===
# JWT secret for session management (use a strong random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# === CORS CONFIGURATION ===
# Client URL for CORS (frontend URL)
# For local development
CLIENT_URL=http://localhost:5173
# For production, this will be set automatically by Render

# === AI FEATURES (Optional) ===
# OpenRouter API key for AI analysis features
OPENROUTER_API_KEY=your-openrouter-api-key

# === CLOUD STORAGE (Optional) ===
# Google Cloud Storage credentials (JSON format)
GOOGLE_APPLICATION_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# === UI FEATURE FLAGS ===
# Enable AI analysis UI indicators on import pages
VITE_AI_ANALYSIS_UI=true

# Enable maintenance/orphan cleanup utilities in UI
VITE_MAINTENANCE_UTILS_UI=true
